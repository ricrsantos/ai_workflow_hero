// Package todos reads pending work from context/current-state.md for /hero-todos
// (TUI palette and Runtime guidance). Parsing and display formatting are shared
// so CLI and TUI stay consistent (UI-C03-001 §6; ADR-028).
package todos
